{% extends 'UMRAMemberBundle::nav.html.twig' %}

{% block modulecontent -%}
    <div class="page-header">
        <h1>Adding a Person to Household of <strong>{{ household.firstname }} {{ household.lastname }}</strong></h1>
    </div>

    <div class="row">
        <div class="col-sm-6">
            {{ form(form) }}
        </div>
    </div>

    <ul class="list-inline">
        <li>
            <a class="btn btn-default" href="{{ path('UMRA_Household_show', { 'id': household.id }) }}">
                Back to household
            </a>
        </li>
    </ul>
{% endblock %}
