Household ID, Postal Name, Last Name, First Name, Nametag Name, PriSec, Active, Primary Email
{% for household in entities %}
{% for member in household.persons %}
"{{ household.id }}","{{ household.postalname }}","{{ member.firstname }}","{{ member.lastname }}","{{ member.nametagname }}","{{ member.secondary ? "Spouse" : "Retiree" }}","{{ member.activenow ? "Yes" : "No" }}","{{ member.emailCanonical }}"
{% endfor %}
{% endfor %}
